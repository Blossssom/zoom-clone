doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
        link(rel="stylesheet", href="https://unpkg.com/mvp.css")
    body 
        header 
            h1 home!!!
        main
            div#rooms
                form
                    input(type="text", placeholder="room name", require)
                    button Enter Room
                h4 Open Rooms :
                ul
            
            div#room
                h3
                ul
                form#nickNameForm
                    input(type="text", placeholder="input nickname")
                    button Save
                form#messageForm
                    input(type="text", placeholder="message", require)
                    button Send

        script(src="/socket.io/socket.io.js")
        //- socket.io를 사용하기 위해 제공된 url 호출 
        script(src="/public/js/app.js")